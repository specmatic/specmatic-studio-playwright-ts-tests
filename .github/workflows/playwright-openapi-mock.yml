name: Studio - OpenAPI Mock Tests
on:
  workflow_dispatch:
    inputs:
      specmatic_jar_url:
        description: Specmatic Enterprise artifact URL
        required: false
        type: string
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
jobs:
  run-mock-server:
    uses: ./.github/workflows/playwright-test-group.yml
    with:
      test_path: specs/openapi/run-mock-server
      artifact_name: run-mock-server
      group_name: Mock
      enterprise_artifact_url: ${{ inputs.specmatic_jar_url || '' }}
    secrets:
      APPLITOOLS_API_KEY: ${{ secrets.APPLITOOLS_API_KEY }}
      SPECMATIC_LICENSE_KEY: ${{ secrets.SPECMATIC_LICENSE_KEY }}
