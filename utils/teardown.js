// Playwright global teardown to ensure browser closes after tests
module.exports = async () => {
  // Playwright closes browsers automatically, but you can add custom cleanup here if needed
  // For example, close any lingering browser instances or clean up resources
};
